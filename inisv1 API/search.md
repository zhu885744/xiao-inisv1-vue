## 搜索服务 API

### 接口概述

搜索服务控制器，提供文章、页面、标签、用户、友链的全局搜索功能。

### 状态码规范

| 状态码 | 含义 | 使用场景 |
| :--- | :--- | :--- |
| 200 | 请求成功 | 搜索成功 |
| 202 | 接受请求 | 无实际功能接口 |
| 400 | 请求参数错误 | 参数校验失败 |
| 405 | 方法不允许 | 调用了不存在的方法 |
| 500 | 服务器错误 | 服务器内部错误 |

### 接口列表

#### 1. 文章搜索 [article]

**请求方式**: `GET`

**请求地址**: `/api/search/article`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| keyword | string | 是 | 搜索关键词 |
| page | int | 否 | 页码，默认1 |
| limit | int | 否 | 每页数量，默认10 |

**响应示例**:

```json
{
  "code": 200,
  "msg": "搜索成功！",
  "data": {
    "data": [
      {
        "id": 1,
        "title": "搜索结果标题",
        "abstract": "文章摘要...",
        "tags": "标签1,标签2",
        "views": 123,
        "create_time": 1699920000
      }
    ],
    "count": 15,
    "page": 1,
    "keyword": "关键词"
  }
}
```

---

#### 2. 页面搜索 [pages]

**请求方式**: `GET`

**请求地址**: `/api/search/pages`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| keyword | string | 是 | 搜索关键词 |
| page | int | 否 | 页码，默认1 |
| limit | int | 否 | 每页数量，默认10 |

---

#### 3. 标签搜索 [tags]

**请求方式**: `GET`

**请求地址**: `/api/search/tags`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| keyword | string | 是 | 搜索关键词 |
| page | int | 否 | 页码，默认1 |
| limit | int | 否 | 每页数量，默认10 |

---

#### 4. 用户搜索 [users]

**请求方式**: `GET`

**请求地址**: `/api/search/users`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| keyword | string | 是 | 搜索关键词 |
| page | int | 否 | 页码，默认1 |
| limit | int | 否 | 每页数量，默认10 |

**响应示例**:

```json
{
  "code": 200,
  "msg": "搜索成功！",
  "data": {
    "data": [
      {
        "id": 1,
        "nickname": "用户昵称",
        "avatar": "头像URL",
        "description": "用户描述",
        "title": "用户头衔",
        "email": "邮箱"
      }
    ],
    "count": 5,
    "page": 1,
    "keyword": "关键词"
  }
}
```

---

#### 5. 友链搜索 [links]

**请求方式**: `GET`

**请求地址**: `/api/search/links`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| keyword | string | 是 | 搜索关键词 |
| page | int | 否 | 页码，默认1 |
| limit | int | 否 | 每页数量，默认10 |

**响应示例**:

```json
{
  "code": 200,
  "msg": "搜索成功！",
  "data": {
    "data": [
      {
        "id": 1,
        "nickname": "友链名称",
        "avatar": "头像URL",
        "description": "友链描述",
        "url": "链接地址",
        "audit": 1
      }
    ],
    "count": 3,
    "page": 1,
    "keyword": "关键词"
  }
}
```

---

#### 6. 全局搜索 [all]

**请求方式**: `GET`

**请求地址**: `/api/search/all`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| keyword | string | 是 | 搜索关键词 |
| page | int | 否 | 页码，默认1 |
| limit | int | 否 | 每页数量，默认10 |

**响应示例**:

```json
{
  "code": 200,
  "msg": "搜索成功！",
  "data": {
    "article": {
      "data": [...],
      "count": 10,
      "page": 1
    },
    "pages": {
      "data": [...],
      "count": 5,
      "page": 1
    },
    "tags": {
      "data": [...],
      "count": 3,
      "page": 1
    },
    "users": {
      "data": [...],
      "count": 2,
      "page": 1
    },
    "links": {
      "data": [...],
      "count": 1,
      "page": 1
    },
    "total": 21,
    "keyword": "关键词"
  }
}
```

### 特殊说明

1. **搜索范围**:
   - 文章搜索：标题、内容、摘要、标签
   - 页面搜索：标题、内容、key
   - 标签搜索：名称、描述
   - 用户搜索：昵称、邮箱、描述、头衔
   - 友链搜索：昵称、描述、链接地址

2. **分页限制**: 全局搜索时每个类型最多返回 limit/5 条结果（最少1条）

3. **审核过滤**:
   - 文章和页面只搜索审核通过的数据（audit = 1）
   - 友链只搜索审核通过的数据（audit = 1）
   - 用户排除冻结用户（status = 0 表示正常）