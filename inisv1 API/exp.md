# EXP API 文档

## 接口概述

EXP 控制器负责用户经验值管理，支持经验值的增删改查、统计聚合，以及点赞、分享、收藏、签到等互动功能。

**接口类型**：基础接口 + 业务接口

---

## 状态码规范

| 状态码 | 使用场景 | 开发注意事项 |
|--------|----------|--------------|
| 200 | 接口调用成功且数据返回正常 | 所有正常响应必须返回200，data为有效业务数据 |
| 202 | 业务逻辑处理完成但有特殊状态 | 如重复签到、重复点赞等 |
| 204 | 接口调用成功但无数据返回 | 空数据场景必须返回204 |
| 400 | 请求参数错误或业务校验失败 | 参数校验失败、必填字段缺失 |
| 401 | 未登录或登录态失效 | 用户未登录时返回 |
| 403 | 无权限访问 | 非root用户访问他人数据时返回 |
| 405 | 请求方法不允许 | 调用了未定义的method |
| 500 | 服务器异常 | 生产环境屏蔽具体错误 |

---

## 接口列表

### 基础接口

#### 1. 获取指定记录 [基础接口-获取指定]

**请求方式**：GET  
**请求路径**：`/api/exp/one`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | int | 是 | 记录ID |
| field | string | 否 | 字段过滤 |

#### 2. 获取记录列表 [基础接口-获取全部]

**请求方式**：GET  
**请求路径**：`/api/exp/all`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | int | 否 | 1 | 页码 |
| field | string | 否 | - | 字段过滤 |
| where | string | 否 | - | 条件查询 |
| order | string | 否 | create_time desc | 排序 |

#### 3. 随机获取 [基础接口-随机获取]

**请求方式**：GET  
**请求路径**：`/api/exp/rand`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| except | string | 否 | 排除ID列表 |
| limit | int | 否 | 返回数量限制 |

#### 4. 查询数量 [基础接口-查询数量]

**请求方式**：GET  
**请求路径**：`/api/exp/count`

#### 5. 求和 [基础接口-求和]

**请求方式**：GET  
**请求路径**：`/api/exp/sum`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| field | string | 是 | 要求和的字段 |

#### 6. 最小值 [基础接口-最小值]

**请求方式**：GET  
**请求路径**：`/api/exp/min`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| field | string | 是 | 要求最小值的字段 |

#### 7. 最大值 [基础接口-最大值]

**请求方式**：GET  
**请求路径**：`/api/exp/max`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| field | string | 是 | 要求最大值的字段 |

#### 8. 查询列 [基础接口-查询列]

**请求方式**：GET  
**请求路径**：`/api/exp/column`

#### 9. 添加记录 [基础接口-添加数据]

**请求方式**：POST  
**请求路径**：`/api/exp/create`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| value | int | 是 | 经验值 |
| type | string | 是 | 类型（like/share/collect/check-in等） |
| description | string | 否 | 描述 |

#### 10. 修改记录 [基础接口-修改数据]

**请求方式**：PUT  
**请求路径**：`/api/exp/update`

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | int | 是 | 记录ID |
| value | int | 否 | 经验值 |
| type | string | 否 | 类型 |

#### 11. 保存记录 [基础接口-保存数据]

**请求方式**：POST  
**请求路径**：`/api/exp/save`

#### 12. 软删除 [基础接口-软删除]

**请求方式**：DELETE  
**请求路径**：`/api/exp/remove`

#### 13. 彻底删除 [基础接口-彻底删除]

**请求方式**：DELETE  
**请求路径**：`/api/exp/delete`

#### 14. 清空回收站 [基础接口-清空回收站]

**请求方式**：DELETE  
**请求路径**：`/api/exp/clear`

#### 15. 恢复数据 [基础接口-恢复数据]

**请求方式**：PUT  
**请求路径**：`/api/exp/restore`

---

### 业务接口

#### 16. 签到 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/check-in`

**说明**：用户每日签到获取经验值（+10 EXP）

**响应示例**：

```json
{
  "code": 200,
  "msg": "签到成功！",
  "data": {
    "value": 10
  }
}
```

#### 17. 点赞 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/like`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| bind_type | string | 否 | article | 类型（article/page/comment） |
| bind_id | int | 是 | - | 绑定ID |
| state | int | 否 | 1 | 状态（0取消/1点赞） |

**响应示例**：

```json
{
  "code": 200,
  "msg": "点赞成功！",
  "data": {
    "value": 1
  }
}
```

#### 18. 分享 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/share`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| bind_type | string | 否 | article | 类型（article/page） |
| bind_id | int | 是 | - | 绑定ID |

**响应示例**：

```json
{
  "code": 200,
  "msg": "分享成功！",
  "data": {
    "value": 1
  }
}
```

#### 19. 收藏 [业务接口]

**请求方式**：POST  
**请求路径**：`/api/exp/collect`

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| bind_type | string | 否 | article | 类型（article/page） |
| bind_id | int | 是 | - | 绑定ID |
| state | int | 否 | 1 | 状态（0取消/1收藏） |

**响应示例**：

```json
{
  "code": 200,
  "msg": "收藏成功！",
  "data": {
    "value": 1
  }
}
```

#### 20. 活跃用户排行 [业务接口]

**请求方式**：GET  
**请求路径**：`/api/exp/active`

**说明**：获取指定时间范围内经验值最多的用户排行

**请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| start | int | 否 | 开始时间戳（默认本月初） |
| end | int | 否 | 结束时间戳（默认本月末） |

**响应示例**：

```json
{
  "code": 200,
  "msg": "数据请求成功！",
  "data": [
    {
      "id": 1,
      "nickname": "用户A",
      "avatar": "...",
      "exp": 1500,
      "count": 25
    }
  ]
}
```

---

## 特殊说明

### 经验值规则

| 行为 | 经验值 | 说明 |
|------|--------|------|
| 签到 | +10 | 每日一次 |
| 点赞 | +1 | 可取消 |
| 分享 | +1 | 每日多次 |
| 收藏 | +1 | 可取消 |

### 权限控制

- 普通用户只能操作自己的数据
- root用户可以操作所有数据

### 缓存机制

- 查询接口支持缓存
- 数据更新后自动清除相关缓存