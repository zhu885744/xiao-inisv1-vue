# Article Group 接口文档

## 接口概述

`article-group` 控制器用于管理文章分组（栏目），支持分组的层级结构管理、CRUD 操作和树形结构展示。所有接口均支持软删除机制和缓存优化。

### 接口类型说明

| 接口类型 | 说明 |
| :--- | :--- |
| **基础接口** | 支持15个基础接口：one、all、rand、count、sum、min、max、column、remove、delete、clear、restore、save、create、update |
| **特殊接口** | tree - 获取树形结构 |

---

## 状态码规范

| 状态码 | 说明 | 使用场景 |
| :--- | :--- | :--- |
| **200** | 请求成功 | 获取数据成功、操作成功 |
| **201** | 创建成功 | 新增数据成功 |
| **204** | 无内容 | 查询无数据、无可操作数据 |
| **400** | 请求错误 | 参数校验失败、操作失败 |
| **401** | 未授权 | 用户未登录 |
| **403** | 无权限 | 无操作权限 |
| **405** | 方法不允许 | 请求方法错误或方法名错误 |
| **500** | 服务器错误 | 系统内部错误 |

---

## 接口列表

### 1. GET 请求接口

#### 1.1 获取单个分组 [基础接口-获取指定]

- **路径**: `/api/article-group/one`
- **方法**: `GET`
- **描述**: 根据条件获取单个文章分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | int | 否 | 分组ID |
| `field` | string | 否 | 返回字段，逗号分隔 |
| `where` | json | 否 | 条件查询 |
| `like` | json | 否 | 模糊查询 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "id": 1,
        "pid": 0,
        "name": "技术文章",
        "key": "tech",
        "description": "技术相关文章",
        "create_time": 1699900000
    }
}
```

#### 1.2 获取所有分组 [基础接口-获取全部]

- **路径**: `/api/article-group/all`
- **方法**: `GET`
- **描述**: 分页获取文章分组列表

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `page` | int | 否 | 页码，默认1 |
| `limit` | int | 否 | 每页数量 |
| `order` | string | 否 | 排序字段，默认 `create_time desc` |
| `field` | string | 否 | 返回字段，逗号分隔 |
| `where` | json | 否 | 条件查询 |
| `like` | json | 否 | 模糊查询 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "data": [...],
        "count": 10,
        "page": 1
    }
}
```

#### 1.3 获取树形结构

- **路径**: `/api/article-group/tree`
- **方法**: `GET`
- **描述**: 获取分组的树形结构

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `pid` | int | 否 | 父级ID，默认0（顶级） |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": [
        {
            "value": 1,
            "label": "技术文章",
            "data": {...},
            "children": [
                {
                    "value": 2,
                    "label": "前端技术",
                    "data": {...},
                    "children": []
                }
            ]
        }
    ]
}
```

#### 1.4 随机获取分组 [基础接口-随机获取]

- **路径**: `/api/article-group/rand`
- **方法**: `GET`
- **描述**: 随机获取指定数量的分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `limit` | int | 否 | 返回数量 |
| `except` | string | 否 | 排除的ID，逗号分隔 |
| `field` | string | 否 | 返回字段 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "好的！",
    "data": [...]
}
```

#### 1.5 查询数量 [基础接口-查询数量]

- **路径**: `/api/article-group/count`
- **方法**: `GET`
- **描述**: 查询分组数量

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `where` | json | 否 | 条件查询 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "查询成功！",
    "data": 10
}
```

#### 1.6 求和 [基础接口-求和]

- **路径**: `/api/article-group/sum`
- **方法**: `GET`
- **描述**: 对指定字段求和

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 求和字段 |
| `where` | json | 否 | 条件查询 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "count": 100
    }
}
```

#### 1.7 最小值 [基础接口-最小值]

- **路径**: `/api/article-group/min`
- **方法**: `GET`
- **描述**: 获取指定字段最小值

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 字段名 |
| `where` | json | 否 | 条件查询 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "create_time": 1699900000
    }
}
```

#### 1.8 最大值 [基础接口-最大值]

- **路径**: `/api/article-group/max`
- **方法**: `GET`
- **描述**: 获取指定字段最大值

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 字段名 |
| `where` | json | 否 | 条件查询 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "create_time": 1700000000
    }
}
```

#### 1.9 查询列 [基础接口-查询列]

- **路径**: `/api/article-group/column`
- **方法**: `GET`
- **描述**: 获取指定字段列表

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 字段名 |
| `where` | json | 否 | 条件查询 |
| `order` | string | 否 | 排序字段 |
| `ids` | string | 否 | 指定ID列表 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": ["技术文章", "生活随笔"]
}
```

---

### 2. POST 请求接口

#### 2.1 保存分组 [基础接口-保存数据]

- **路径**: `/api/article-group/save`
- **方法**: `POST`
- **描述**: 保存分组（新增或更新）

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | int | 否 | 分组ID，为空时新增 |
| `pid` | int | 否 | 父级ID，默认0 |
| `name` | string | **是** | 分组名称 |
| `key` | string | 否 | 分组标识 |
| `description` | string | 否 | 分组描述 |
| `avatar` | string | 否 | 分组图标 |
| `json` | json | 否 | JSON数据 |
| `text` | string | 否 | 文本内容 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "创建成功！",
    "data": {
        "id": 1
    }
}
```

#### 2.2 创建分组 [基础接口-添加数据]

- **路径**: `/api/article-group/create`
- **方法**: `POST`
- **描述**: 新增文章分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `pid` | int | 否 | 父级ID，默认0 |
| `name` | string | **是** | 分组名称 |
| `key` | string | 否 | 分组标识 |
| `description` | string | 否 | 分组描述 |
| `avatar` | string | 否 | 分组图标 |
| `json` | json | 否 | JSON数据 |
| `text` | string | 否 | 文本内容 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "创建成功！",
    "data": {
        "id": 1
    }
}
```

---

### 3. PUT 请求接口

#### 3.1 更新分组 [基础接口-修改数据]

- **路径**: `/api/article-group/update`
- **方法**: `PUT`
- **描述**: 更新分组信息

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | int | **是** | 分组ID |
| `pid` | int | 否 | 父级ID |
| `name` | string | 否 | 分组名称 |
| `key` | string | 否 | 分组标识 |
| `description` | string | 否 | 分组描述 |
| `avatar` | string | 否 | 分组图标 |
| `json` | json | 否 | JSON数据 |
| `text` | string | 否 | 文本内容 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "更新成功！",
    "data": {
        "id": 1
    }
}
```

#### 3.2 恢复分组 [基础接口-恢复数据]

- **路径**: `/api/article-group/restore`
- **方法**: `PUT`
- **描述**: 从回收站恢复分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `ids` | string | **是** | 分组ID列表，逗号分隔 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "恢复成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

---

### 4. DELETE 请求接口

#### 4.1 软删除分组 [基础接口-软删除]

- **路径**: `/api/article-group/remove`
- **方法**: `DELETE`
- **描述**: 将分组移入回收站

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `ids` | string | **是** | 分组ID列表，逗号分隔 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "删除成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

#### 4.2 彻底删除分组 [基础接口-彻底删除]

- **路径**: `/api/article-group/delete`
- **方法**: `DELETE`
- **描述**: 永久删除分组，不可恢复

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `ids` | string | **是** | 分组ID列表，逗号分隔 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "删除成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

#### 4.3 清空回收站 [基础接口-清空回收站]

- **路径**: `/api/article-group/clear`
- **方法**: `DELETE`
- **描述**: 清空所有已删除的分组

**请求参数**: 无

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "清空成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

---

## 特殊说明

### 1. 树形结构
- `tree` 接口返回嵌套的树形结构，适用于层级分类展示
- 支持递归查询子分组

### 2. 字段处理
- `json` 和数组类型字段会自动序列化

### 3. 缓存策略
- 所有查询接口均支持缓存
- 数据修改后会自动清除相关缓存