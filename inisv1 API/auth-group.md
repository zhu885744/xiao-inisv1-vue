# Auth Group 接口文档

## 接口概述

`auth-group` 控制器用于管理用户权限分组，支持分组的创建、查询、更新和删除操作。包含特殊的用户关联功能，禁止删除系统管理员分组。

### 接口类型说明

| 接口类型 | 说明 |
| :--- | :--- |
| **基础接口** | 支持15个基础接口：one、all、rand、count、sum、min、max、column、remove、delete、clear、restore、save、create、update |
| **特殊接口** | uids - 用户分组关联 |

---

## 状态码规范

| 状态码 | 说明 | 使用场景 |
| :--- | :--- | :--- |
| **200** | 请求成功 | 获取数据成功、操作成功 |
| **201** | 创建成功 | 新增数据成功 |
| **204** | 无内容 | 查询无数据、无可操作数据 |
| **400** | 请求错误 | 参数校验失败、操作失败 |
| **401** | 未授权 | 用户未登录 |
| **403** | 无权限 | 无操作权限、禁止删除系统分组 |
| **405** | 方法不允许 | 请求方法错误或方法名错误 |
| **500** | 服务器错误 | 系统内部错误 |

---

## 接口列表

### 1. GET 请求接口

#### 1.1 获取单个分组 [基础接口-获取指定]

- **路径**: `/api/auth-group/one`
- **方法**: `GET`
- **描述**: 根据条件获取单个权限分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | int | 否 | 分组ID |
| `field` | string | 否 | 返回字段，逗号分隔 |
| `where` | json | 否 | 条件查询 |
| `like` | json | 否 | 模糊查询 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "id": 1,
        "name": "管理员",
        "key": "admin",
        "root": 1,
        "create_time": 1699900000
    }
}
```

#### 1.2 获取所有分组 [基础接口-获取全部]

- **路径**: `/api/auth-group/all`
- **方法**: `GET`
- **描述**: 分页获取权限分组列表

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `page` | int | 否 | 页码，默认1 |
| `limit` | int | 否 | 每页数量 |
| `order` | string | 否 | 排序字段，默认 `create_time desc` |
| `field` | string | 否 | 返回字段，逗号分隔 |
| `where` | json | 否 | 条件查询 |
| `like` | json | 否 | 模糊查询 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "data": [...],
        "count": 10,
        "page": 1
    }
}
```

#### 1.3 随机获取分组 [基础接口-随机获取]

- **路径**: `/api/auth-group/rand`
- **方法**: `GET`
- **描述**: 随机获取指定数量的分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `limit` | int | 否 | 返回数量 |
| `except` | string | 否 | 排除的ID，逗号分隔 |
| `field` | string | 否 | 返回字段 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "好的！",
    "data": [...]
}
```

#### 1.4 查询数量 [基础接口-查询数量]

- **路径**: `/api/auth-group/count`
- **方法**: `GET`
- **描述**: 查询分组数量

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `where` | json | 否 | 条件查询 |
| `withTrashed` | bool | 否 | 是否包含已删除数据 |
| `onlyTrashed` | bool | 否 | 是否只查询已删除数据 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "查询成功！",
    "data": 10
}
```

#### 1.5 求和 [基础接口-求和]

- **路径**: `/api/auth-group/sum`
- **方法**: `GET`
- **描述**: 对指定字段求和

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 求和字段 |
| `where` | json | 否 | 条件查询 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "count": 100
    }
}
```

#### 1.6 最小值 [基础接口-最小值]

- **路径**: `/api/auth-group/min`
- **方法**: `GET`
- **描述**: 获取指定字段最小值

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 字段名 |
| `where` | json | 否 | 条件查询 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "create_time": 1699900000
    }
}
```

#### 1.7 最大值 [基础接口-最大值]

- **路径**: `/api/auth-group/max`
- **方法**: `GET`
- **描述**: 获取指定字段最大值

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 字段名 |
| `where` | json | 否 | 条件查询 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": {
        "create_time": 1700000000
    }
}
```

#### 1.8 查询列 [基础接口-查询列]

- **路径**: `/api/auth-group/column`
- **方法**: `GET`
- **描述**: 获取指定字段列表

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `field` | string | **是** | 字段名 |
| `where` | json | 否 | 条件查询 |
| `order` | string | 否 | 排序字段 |
| `ids` | string | 否 | 指定ID列表 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "数据请求成功！",
    "data": ["管理员", "普通用户"]
}
```

---

### 2. POST 请求接口

#### 2.1 保存分组 [基础接口-保存数据]

- **路径**: `/api/auth-group/save`
- **方法**: `POST`
- **描述**: 保存分组（新增或更新）

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | int | 否 | 分组ID，为空时新增 |
| `name` | string | **是** | 分组名称 |
| `key` | string | 否 | 分组标识 |
| `rules` | string | 否 | 权限规则ID列表，逗号分隔 |
| `uids` | string | 否 | 用户ID列表，逗号分隔 |
| `root` | int | 否 | 是否超级管理员组，0/1 |
| `pages` | string | 否 | 关联页面ID列表 |
| `remark` | string | 否 | 备注 |
| `json` | json | 否 | JSON数据 |
| `text` | string | 否 | 文本内容 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "创建成功！",
    "data": {
        "id": 1
    }
}
```

#### 2.2 创建分组 [基础接口-添加数据]

- **路径**: `/api/auth-group/create`
- **方法**: `POST`
- **描述**: 新增权限分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `name` | string | **是** | 分组名称 |
| `key` | string | 否 | 分组标识 |
| `rules` | string | 否 | 权限规则ID列表，逗号分隔 |
| `uids` | string | 否 | 用户ID列表，逗号分隔 |
| `root` | int | 否 | 是否超级管理员组，0/1 |
| `pages` | string | 否 | 关联页面ID列表 |
| `remark` | string | 否 | 备注 |
| `json` | json | 否 | JSON数据 |
| `text` | string | 否 | 文本内容 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "创建成功！",
    "data": {
        "id": 1
    }
}
```

---

### 3. PUT 请求接口

#### 3.1 更新分组 [基础接口-修改数据]

- **路径**: `/api/auth-group/update`
- **方法**: `PUT`
- **描述**: 更新分组信息

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | int | **是** | 分组ID |
| `name` | string | 否 | 分组名称 |
| `key` | string | 否 | 分组标识 |
| `rules` | string | 否 | 权限规则ID列表 |
| `uids` | string | 否 | 用户ID列表 |
| `root` | int | 否 | 是否超级管理员组 |
| `pages` | string | 否 | 关联页面ID列表 |
| `remark` | string | 否 | 备注 |
| `json` | json | 否 | JSON数据 |
| `text` | string | 否 | 文本内容 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "更新成功！",
    "data": {
        "id": 1
    }
}
```

#### 3.2 恢复分组 [基础接口-恢复数据]

- **路径**: `/api/auth-group/restore`
- **方法**: `PUT`
- **描述**: 从回收站恢复分组

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `ids` | string | **是** | 分组ID列表，逗号分隔 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "恢复成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

#### 3.3 用户分组关联

- **路径**: `/api/auth-group/uids`
- **方法**: `PUT`
- **描述**: 批量更新用户的分组关联

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `uid` | int | **是** | 用户ID |
| `ids` | string | 否 | 分组ID列表，逗号分隔 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "更新成功！",
    "data": null
}
```

**特殊说明**: 
- 该接口会从其他分组中移除该用户，然后添加到指定分组
- 异步更新用户缓存

---

### 4. DELETE 请求接口

#### 4.1 软删除分组 [基础接口-软删除]

- **路径**: `/api/auth-group/remove`
- **方法**: `DELETE`
- **描述**: 将分组移入回收站

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `ids` | string | **是** | 分组ID列表，逗号分隔 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "删除成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

**失败响应** (403):
```json
{
    "code": 403,
    "msg": "禁止删除系统管理员分组！",
    "data": null
}
```

**特殊说明**: 系统管理员分组（ID=1）和默认分组禁止删除

#### 4.2 彻底删除分组 [基础接口-彻底删除]

- **路径**: `/api/auth-group/delete`
- **方法**: `DELETE`
- **描述**: 永久删除分组，不可恢复

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `ids` | string | **是** | 分组ID列表，逗号分隔 |

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "删除成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

**特殊说明**: 系统管理员分组和默认分组禁止删除

#### 4.3 清空回收站 [基础接口-清空回收站]

- **路径**: `/api/auth-group/clear`
- **方法**: `DELETE`
- **描述**: 清空所有已删除的分组

**请求参数**: 无

**成功响应** (200):
```json
{
    "code": 200,
    "msg": "清空成功！",
    "data": {
        "ids": [1, 2, 3]
    }
}
```

---

## 特殊说明

### 1. 系统分组保护
- ID=1 的系统管理员分组禁止删除
- 默认分组（default=1）禁止删除

### 2. 用户关联机制
- `uids` 接口支持批量更新用户分组关联
- 自动从其他分组中移除用户，再添加到新分组

### 3. 缓存策略
- 所有查询接口均支持缓存
- 数据修改后会自动清除相关缓存

### 4. 字段处理
- `rules`、`uids`、`pages` 字段支持数组格式，会自动序列化为 `|1|2|3|` 格式